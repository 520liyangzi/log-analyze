# 第一版验证记录

验证环境：Linux，Python 3.12.14，Node.js 24.19.0。没有真实业务日志包，测试基于用户提供的两种日志文本、目录约定及生成的模拟包。

## 已执行并通过

命令：`python -m unittest discover -s tests -v`

13 项测试：

1. 用户提供的 access 格式、转义引号、线程、状态码和末列耗时。
2. 用户提供的 root 格式、19 位流水号原样保留、带时区时间解析。
3. 多 Node 嵌套 ZIP、GZIP 历史记录和完整来源路径。
4. Node / Pod / 类型 / 文件名 / 状态 / 耗时 AND 筛选、中文和连续文本匹配。
5. 精确流水号跨 Pod 追踪、Java 多行异常堆栈与文件上下文。
6. 同节点、时间范围、线程组合关联。
7. 160 条结果跨 4 页读取，无重复或遗漏，全量 NDJSON 导出相同记录。
8. 缺少 FTS 时扫描降级仍正确。
9. 按文件 ID 限定一个来源文件。
10. 损坏 GZIP 导入整体回滚，记录错误状态，不留下部分日志/索引。
11. GB18030 中文解码、ZIP 路径不写入宿主路径。
12. 真实本地 HTTP 上传、搜索和首页响应；跨 Origin / 非法 Host 拒绝；AI 缺少发送确认拒绝。
13. AI 模拟服务：接口检索优先错误请求，关联异常证据，构造兼容 chat completions 的请求。

另已执行：

- `python -m py_compile app.py demo.py`
- `node --check dist/app.js`
- 静态入口引用检查：HTML 引用 `/app.js` 和 `/style.css`，均由 HTTP 服务提供；无 CDN、网络字体或前端依赖。

## 未验证范围

- 未用用户的真实 ZIP 验证，实际日志字段或目录变体可能需要补充规则。
- 未执行浏览器交互/截图测试；前端进行了语法和静态入口检查。
- 未在 Windows 实机运行；提供 Windows 启动脚本和跨系统自动测试配置。
- 未连接真实模型服务；只验证了检索证据与模拟模型响应。
- 未做 GB 级性能压测，不能给出大包导入耗时或搜索速度承诺。

复现：`python demo.py` 生成模拟包后，按照 README「先跑一次演示」逐步操作。

## v1.1 网页终端与 Skill

新增 6 项测试（合计 19 项）：

- 原 ZIP/GZIP 回读核验，故意更改索引文本后正确识别不一致。
- CLI 接口搜索 → 关联 root → 原文核验 → 流水号追踪 → 全量导出；检查分页元信息和扫描回退。
- 旧版未保留原 ZIP 时明确返回不可核验。
- Skill 可独立安装与执行，拒绝覆盖已有目录。
- 真实 PTY：子程序 `isatty()` 为真，输入中文、读取回复、调整窗口、Ctrl+C 中断等待、读取报告、结束会话。测试目录包含空格。
- 终端启动 API 拒绝来自其他 Origin 的请求。

本地 Linux 已通过以上测试；CI 配置在 Windows/Linux × Python 3.10/3.12 上安装依赖并执行。没有调用真实 Claude 或公司内部 Agent；不声明已经验证这些程序的登录、授权及版本差异。未执行浏览器截图/交互测试。

## v1.2 样例适配

新增 4 个格式/流程回归用例，总计 23 项：含下划线 namespace/Pod/service 拆分，结构化字段/两种日期写法，HTTP 200 下的多类型异步错误与清单核对，以及旧解析器提示。实际附件的 16 文件、62 记录全部回读核验通过，详情见 [SAMPLE_VALIDATION.md](SAMPLE_VALIDATION.md)。

## v1.3 可编辑规则与便捷 AI 排查

本地 Linux / Python 3.12 全部 26 项测试通过。新增 3 项集成测试覆盖：

- 规则保存、历史不可变、旧版本写入冲突、恢复默认仍保留历史、重建存储对象后规则仍在、空流程/超长流程被拒绝。
- 任务预览不启动进程；创建任务使用选定版本，之后保存新规则不会改变原任务；显式规则更新保存独立文件并记录版本，已结束会话不能更新。
- 使用真实 PTY 启动确定性测试程序：自动模式只传入固定读取任务参数，含中文/引号/shell 元字符的问题只存在文件；程序调用本次 tools/logscope.py 检索并核验原包、生成报告、接收中文追问；兼容模式不追加初始参数。

这是启动传输和工具链验证，不是对模型推理质量的测试；未连接真实 Claude 或公司 AI。JavaScript 语法、静态页面资源及控件引用已检查，Skill frontmatter 校验通过。未执行浏览器交互或截图测试。Windows/Linux × Python 3.10/3.12 由仓库 CI 执行。

同版本再次导入用户脱敏附件：16 份文件、62 条记录全部回读原包核验通过；任务目录内复制的 CLI 可正常读取规则。附件没有提交到仓库。

## 仓库自带综合测试日志

`sample_logs.py` 可生成完全虚构的 `generated-log-sample.zip`，目录层级和四类日志格式与脱敏附件保持一致。自动测试每次临时生成并导入，不向仓库提交 ZIP。生成结果为 2 个 Node、2 个 Pod、16 份日志文件、33 个物理行、30 条合并记录，fileList 与实际文件 16/16 一致，30 条记录全部从外层 ZIP 逐层回读核验通过。

端到端测试覆盖全局及 Node/Pod/类型组合搜索、精确接口、5xx/耗时解析、GZIP 来源、完整流水号跨节点追踪、RouteID 精确匹配、多类型时间关联，以及同线程筛选会排除异步 ERROR 的行为。
