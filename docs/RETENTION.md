# 日志保留与自动清理

LogScope v2.1 默认每天服务电脑本地时间 **02:00** 清理已过期的日志索引。原始 ZIP 比索引小得多，因此只清理可重新生成的数据库内容，不自动删除 ZIP。

## 怎么算过期

- 新导入的日志包，从**导入完成时间**算起，达到 **72 小时**后进入下一次定时清理的候选范围。
- 不使用日志内的业务事件时间；上传去年的日志，也有从本次导入完成开始的 72 小时。
- 旧版数据没有导入完成时间时，回退使用该日志包原来的创建时间；升级不会给旧索引重新开始计时。
- 崩溃或中断导入留下的失败记录和部分索引，也按原创建时间计算，超过 72 小时后清理，避免失败数据长期占盘。
- 72 小时是过期门槛，不是精确删除时刻。每日统一检查一次，因此实际保留时间通常为 72～96 小时；运行中的任务、停机和清理重试可能进一步延后。

例如周一上午 10:00 导入完成，周四上午 10:00 满 72 小时；如果期间服务一直运行且没有任务阻碍，将在周五凌晨 02:00 清理。

## 哪些保留，哪些清理

| 内容 | 自动清理行为 |
|---|---|
| 过期日志包的日志记录、文件解析元数据、全文检索索引 | 清理 |
| 创建时间超过 72 小时的失败导入所残留的部分索引 | 清理 |
| 未过期日志包及其索引 | 保留，正常使用 |
| 上传或在线采集得到的原始 ZIP | 保留在数据目录的 `archives/<日志包ID>.zip` |
| 过期日志包名称、ZIP 大小、归档路径与清理时间 | 保留，可在页面查看 |
| AI 对话、报告、旧终端会话与任务记录 | 保留 |
| 模型 API 配置、采集环境、分析规则 | 保留 |

`logs.sqlite3` 是共享数据库容器，不会直接删除整个数据库文件。程序会删除过期日志包对应的记录与索引，然后执行数据库物理压缩（SQLite `VACUUM`）回收空闲页，因此 `data` 中依然会有数据库文件。未过期日志、归档清单等仍需要它。

原始 ZIP 的“保留”仅指不删除**已经存在的文件**，不会自动找回原本缺失的 ZIP。历史版本、失败或中断导入可能根本没有完成原 ZIP 留存；这类记录会显示“原 ZIP 未留存，请使用原上传文件”，不提供下载按钮。

这是**释放磁盘空间**的功能，不是直接清空程序内存，也不会因为某个日志包到期就关闭 AI 会话。

## 调度、停机与正在使用的任务

时间以**运行 LogScope 的服务电脑**为准，不是同事浏览器所在电脑的时区。左侧状态显示下次检查时间及其 UTC 偏移，悬停可查看服务时区。

- 程序运行时，在每天本地时间 02:00 自动检查。
- 程序关闭时不能清理；错过的定时检查会在下次启动时补跑，不需要 Windows 任务计划或另外启动脚本。
- 当前有导入、查询或 AI 排查任务时，先延期 **15 分钟**再检查，不打断正在使用的任务。
- 清理及数据库压缩期间会短暂暂停日志查询，页面有维护提示，请稍后再搜索。清理耗时与数据库体积和磁盘性能有关，大数据库压缩可能需要数分钟。
- 清理完成后，打开的页面会通过约 **30 秒**一次的状态检查刷新日志包列表，自动清掉已失效的旧搜索结果；不需要一直手动刷新。

压缩数据库需要一定临时磁盘空间。请不要等磁盘完全写满才处理。除了正在运行的任务，磁盘余量不足、数据库忙或压缩失败也可能导致延期；页面会显示具体原因并安排重试，请根据页面状态处理。不要在运行中手动删除 `logs.sqlite3` 或其伴随文件。

## 以后还想用这个日志包

1. 点击左侧 **查看保留 ZIP**。
2. 根据原日志包名称找到归档，查看大小、清理时间和归档相对路径。
3. 点击 **下载原始 ZIP**；文件会下载到当前使用浏览器的电脑。
4. 点击 **导入日志包**，上传刚下载的 ZIP，重新建立索引。新日志包会获得新的 72 小时保留期。
5. 需要 AI 分析时，选择新导入的日志包，新建排查。

也可以在服务电脑直接找到 `data/archives/<日志包ID>.zip` 再上传；如果用了 `--data`，则路径相对于你指定的数据目录。页面显示的原始名称和磁盘上的 ID 文件名不同，这是正常的。

旧 AI 会话仍能回看之前的文字、报告和已保存的工具结果，但是被清理的索引已不存在，**旧证据的日志详情、重新查询与继续基于旧日志的排查不可用**。重新导入会生成新日志包 ID，不会偷偷改变旧会话的证据范围，建议对新日志包新建排查。

## 和手动删除的区别

**自动清理只删除可重新生成的日志数据库内容，保留原始 ZIP。**

左侧原有 **删除当前日志包** 仍沿用手动删除语义：删除该包的原始 ZIP、日志记录和索引；确认框中会说明后果。请不要用手动删除替代“等待自动过期”，除非你确实连原 ZIP 也不需要。

## 升级需要重新导入吗

**不需要事先重新导入。** 关闭旧程序后替换 EXE，或更新源码，继续使用原来的 `data` 目录即可。升级会补齐所需的元数据，不会重建全部索引。

注意，旧数据的保留时间仍从原创建时间算起。启动补检查可能清理已经超过 72 小时的索引；原 ZIP 会保留在归档中，需要时再导入。原生 AI、旧终端记录、模型配置、采集环境都可以继续使用。
