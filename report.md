# LogScope AI 排查流程与耗时调查报告

## 调查前提

当前执行目录没有用户消息所指的 `task.md`，因此无法读取某个真实日志任务、调用其中绑定的数据集查询。本文依据项目现有任务生成器、查询脚本、SQLite 查询路径和页面交互完成代码级调查，并记录本轮实现结果。

## 结论

导入完成后已经建立 SQLite 结构化索引和 FTS5 trigram 全文索引。AI 排查不会重新解压日志包，也不会重新建立索引。用户感受到的主要等待来自本机 AI 进程启动、模型推理以及模型逐轮决定并调用 `datasets/files/search/correlate/context/verify`；其中原 ZIP 核验需要重新流式读取对应压缩成员，但普通搜索直接查询数据库。

原流程虽然已经提供“预览任务”按钮，但“开始排查”仍会直接创建任务并启动 AI，预览不是必经步骤，容易让人误以为点击后只是搜索。默认规则还要求先确认范围再逐步关联和核验；如果 Agent 机械地逐条执行示例命令，会发生多轮模型等待。

接口精确搜索原先对 `url` 使用 `substr(...)` 表达式，SQLite 无法利用普通 URL 索引；大数据集上即使已经导入，也可能扫描该日志包的大量记录。这是代码侧可以直接优化的耗时点。

## 本轮改进

1. “开始排查”改为强制两次确认：先生成并展示最终 `task.md`，用户确认后才启动本机 AI。
2. `task.md` 内增加日志包范围快照，以及从问题中提取的接口路径和长流水号线索。默认提示词要求优先执行一条组合索引查询，不再无目的重复枚举日志包和文件。
3. 新增 `(dataset, url)` 索引，并把精确接口查询改为可使用索引的精确值/查询参数范围条件。
4. 日志报告生成后显示“继续结合代码定位”。用户填写本机 Git 项目绝对路径并选择分支，页面预览 `code-task.md` 后才发送给当前 AI。
5. 代码定位固定到预览时的 commit。`tools/project.py` 仅提供 info、tree、grep、show、log 只读查询，不 checkout 分支、不修改工作区、不执行项目构建或测试；如果分支在预览后移动，必须重新预览。
6. 页面直接展示四阶段流程，并说明“索引搜索”和“AI 总耗时”不是一回事。

## 实际提示词

启动命令只附带固定短提示：

```text
Read task.md in the current directory. Use its rules and query tools to investigate the question. Reply in Chinese and write report.md.
```

完整提示词位于页面预览的 `task.md`，由以下内容组成：任务 JSON、日志范围、自动提取的查询线索、版本化分析规则、公司业务规则和只读查询工具说明。这样用户问题不会拼入 shell 命令，且可以在发送前完整核对。

代码定位阶段发送另一条固定短提示，完整内容位于预览后的 `code-task.md`；AI 同时读取已有 `report.md`，只对日志结论涉及的接口、类、方法和错误文本做定向代码搜索。

## 建议的使用流程

先在普通搜索页验证一个接口或流水号是否能命中；再进入 AI 排查，预览并确认任务。AI 完成日志报告后，如果结论已经足够就结束；只有需要追到实现原因时，再选择项目和分支进入代码定位。这样“快速搜日志”和“深度定位问题”分层，既保留速度，也让 AI 在需要时完成从日志证据到代码路径的闭环。
